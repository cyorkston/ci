# CI
ci.sf.org.username = cyorkston@gmail.com.jenkinsciuser
ci.sf.org.password = test@123
ci.sf.org.token = 2sERQzQeRtnLLYfslx1IzeuJV
ci.sf.org.serverurl = https://login.salesforce.com
ci.sf.org.checkonly = false
ci.sf.org.destructiveChanges = false
ci.sf.org.forcetests = true

# Setup polling params
sf.maxPoll = 500
sf.maxWaitMillis = 5000

# More optional params
# sf.pkgName = <Insert comma separated package names to be retrieved>
# sf.pkgContents = applications/*.*, classes/*.*, components/*.*, documents/**/*.*, email/**/*.*, groups/*.*, labels/*.*, layouts/*.*, letterhead/*.*, objects/*.*, pages/*.*, profiles/*.*, reports/**/*.*, reportTypes/*.*, roles/*.*, staticresources/*.*, tabs/*.*, triggers/*.*, weblinks/*.*, workflows/*.*, package.xml
# removed layouts, reports, profiles
# sf.pkgContents = applications/*.*, classes/*.*, components/*.*, documents/**/*.*, email/**/*.*, groups/*.*, labels/*.*, letterhead/*.*, objects/*.*, pages/*.*, reportTypes/*.*, roles/*.*, staticresources/*.*, tabs/*.*, triggers/*.*, weblinks/*.*, workflows/*.*, package.xml
sf.pkgContents = classes/*.*, components/*.*, email/**/*.*, groups/*.*, labels/*.*, objects/*.*, pages/*.*, reportTypes/*.*, staticresources/*.*, tabs/*.*, triggers/*.*, weblinks/*.*, workflows/*.*, package.xml
# sf.pkgContentsDestructive = ${sf.pkgContents}, destructiveChanges.xml
# sf.zipFile = <Insert path of the zipfile to be retrieved>
# sf.metadataType = <Insert metadata type name for which listMetadata or bulkRetrieve operations are to be performed>

# If your network requires an HTTP proxy, see http://ant.apache.org/manual/proxy.html for configuration.
